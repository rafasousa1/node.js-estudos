function soma(a, b) {
    return a + b
}

module.exports = soma // Podemos exportar algum objeto aqui com o common js, no caso a função 'soma', e conseguimos exportar em um outro arquivo qualquer como o 'main.js'